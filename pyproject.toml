[project]
name = "spicy-regs"
version = "0.1.0"
description = "Regulations Data Analysis Tools"
requires-python = ">=3.10"
dependencies = [
    "boto3",
    "python-dotenv",
    "polars",
    "pyarrow",
    "tqdm",
    "httpx",
    "duckdb",
    "pandas",
    "prefect",
    "cyclopts",
    "loguru",
    "mcp>=1.10.0",
]

[project.optional-dependencies]
embed = [
    "sentence-transformers",
    "torch",
]

[dependency-groups]
dev = [
    "ipykernel",
    "matplotlib",
    "pyrefly",
    "pytest",
    "ruff",
    "scikit-learn",
    "seaborn",
    "textblob",
]

[project.scripts]
etl = "spicy_regs.pipeline.pipeline:app"
run-pipeline = "spicy_regs.pipelines.run:app"
embed = "spicy_regs.vectordb.embed:main"
spicy-regs = "spicy_regs.cli:main"
spicy-regs-mcp = "spicy_regs.mcp_server:main"

[build-system]
requires = ["uv_build"]
build-backend = "uv_build"

[tool.ruff]
line-length = 120
target-version = "py312"
exclude = [
    "notebooks"
]

[tool.pyrefly]
project-includes = [
    "**/*.py*"
]
