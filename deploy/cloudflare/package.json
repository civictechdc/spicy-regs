{
  "name": "spicy-regs-mcp-container",
  "private": true,
  "description": "Cloudflare Containers deploy for the Spicy Regs MCP server",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.0.40"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
